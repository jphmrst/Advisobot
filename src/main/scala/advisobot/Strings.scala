package advisobot.core

/** Separate storage for strings.  Some of the LaTeX strings throw off
 *  syntax highlighting, so it's nice to have these in a completely
 *  separate file.
 */
object Strings {
  val ugCheck:String = """\""" + """ugCheck"""
}
